# AWS Compliance Evidence Collector — Dependencies
# Install: pip install -r requirements.txt

# AWS SDK
boto3>=1.34.0
botocore>=1.34.0

# PDF report generation
reportlab>=4.0

# Testing
pytest>=8.0
pytest-cov>=5.0
moto[securityhub,config,iam,dynamodb,s3,sts]>=5.0

# Linting (optional, used by CI)
ruff>=0.4.0
